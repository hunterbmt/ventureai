# ventureai
An AI powered innovation team that will help to to brainstorm and solve challenging topic


## Setup

1. If you don’t have Node.js installed, [install it from here](https://nodejs.org/en/) (Node.js version >= 14.6.0 required)

2. Clone this repository

3. Navigate into the project directory

   ```bash
   $ cd openai-quickstart-node
   ```

4. Install the requirements

   ```bash
   $ npm install
   ```

5. Make a copy of the example environment variables file

   On Linux systems: 
   ```bash
   $ cp .env.example .env
   ```
   On Windows:
   ```powershell
   $ copy .env.example .env
   ```
6. Add your [API key](https://platform.openai.com/account/api-keys) to the newly created `.env` file

7. Config your OBJECTIVE, AGENT_MODEL, MEMORY_MODEL in `.env` file

7. Run the app

   ```bash
   $ npm run start
   ```


## Example

Transcript of example runs could be found in `example_run` folder